<context path="docs/" updated="2026-06-02">

Project documentation describing architecture, data flow, caching strategy, and route patterns. Read by developers and AI agents to understand the system design.

## Summary

- `architecture.md` — Comprehensive architecture document: high-level data flow (Chess.com API -> chessclub library -> sync worker -> SQLAlchemy DB -> Flask routes), application factory, blueprint map with all routes, DB-first route pattern, authentication (server cookies + OAuth PKCE), ORM models, data access layer with pagination support, sync worker (two-phase), template hierarchy, Jinja2 filters, configuration reference, dependency on chessclub library, security considerations.
- `cache.md` — Two-tier cache architecture: Tier 1 (chessclub library SQLite cache, TTL-based, at `~/.cache/chessclub/cache.db`) and Tier 2 (SQLAlchemy persistence, permanent, at `instance/chessclub.db`). Details TTL rules, cache key format, write strategies, computed aggregates, failure modes, data flows for watched vs. non-watched clubs.

## Constraints

- MUST NOT commit secrets, tokens, or passwords to any documentation file.
- MUST keep route map in `architecture.md` synchronized with actual blueprint registrations.

## Guidance

- Documentation SHOULD be updated when a new route, model, or service is added.
- The route map table SHOULD list every route with its HTTP method, path, and handler function.

</context>
