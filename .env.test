# Test environment — copy to .env and adjust as needed.
# Usage: docker compose -f docker-compose.yml -f docker-compose.test.yml up -d --build

SECRET_KEY=test-secret-not-for-production
CHESSCOM_OAUTH_CLIENT_ID=
OAUTH_REDIRECT_URI=http://localhost:5001/auth/callback
CHESSCOM_SERVER_ACCESS_TOKEN=
CHESSCOM_SERVER_PHPSESSID=
SYNC_INTERVAL_HOURS=24
ADMIN_PASSWORD=test-admin
DATABASE_URI=sqlite:////app/instance/chessclub.db
