Flask>=3.0
Flask-SQLAlchemy>=3.1
python-dotenv>=1.0
requests>=2.31
APScheduler>=3.10
portalocker>=3.2
gunicorn>=22.0

# chessclub library — use one of the options below:
# Option A: install from GitHub (production/test) — pin to a commit for reproducibility:
# chessclub @ git+https://github.com/cmellojr/chessclub.git@<commit-sha>
# Option B: install locally (development) — comment line above and run:
#   pip install -e ../chessclub
chessclub @ git+https://github.com/cmellojr/chessclub.git
