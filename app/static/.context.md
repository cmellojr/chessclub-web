<context path="app/static/" updated="2026-06-02">

Static assets served by Flask. Currently contains a single custom CSS file — all base styling is handled by Bootstrap 5.3 loaded via CDN in templates.

## Summary

- `css/custom.css` — Minimal custom CSS overrides: tighter table cell padding, nav-tab active weight emphasis, progress bar background color, responsive heading size on small screens.

## Constraints

- MUST NOT contain any JavaScript files — all JS comes from Bootstrap CDN bundle.

## Guidance

- CSS customizations SHOULD be minimal; prefer Bootstrap utility classes and CDN defaults.

</context>
